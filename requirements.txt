# Minimal required packages for the pipeline
pandas>=1.3
numpy>=1.21
scikit-learn>=1.0

# Optional (recommended for better performance)
xgboost>=1.6  # optional but recommended — pipeline will use XGBRegressor if installed

# Useful extras for analysis / plotting (optional)
matplotlib>=3.4
seaborn>=0.11
